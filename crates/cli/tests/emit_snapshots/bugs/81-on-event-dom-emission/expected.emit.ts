// === input.svelte ===
// generated by svelte-check-native; do not edit
    type Processor = 'stripe' | 'liberapay';
async function $$render_bf7123b7() {

    // Pre-fix: our DOM-element emission silently dropped Svelte-4
    // `on:event={fn}` directives, so reassignments inside the user's
    // handler were invisible to TS's flow analysis — `processor` got
    // narrowed to its initial literal `"stripe"` and `processor ===
    // "liberapay"` then fired TS2367 "no overlap." Now we emit
    // `"on:click": (handler)` as a key in the createElement attribute
    // literal (matches upstream svelte2tsx's EventHandler.ts DOM
    // branch), so TS sees the assignment and de-narrows.

                                            
    let processor: Processor = $state('stripe');
    ;(async () => {
        // template type-check body (incremental)
        { svelteHTML.createElement("button", {
            "on:click": (() => (processor = 'stripe')),
        });
            (processor === 'stripe');
        }
        { svelteHTML.createElement("button", {
            "on:click": (() => (processor = 'liberapay')),
        });
            (processor === 'liberapay');
        }
        { svelteHTML.createElement("button", {
            "on:click": (() => (processor = 'stripe')),
            "on:click": undefined,
        });
        }
    });
    void processor;
    return { props: undefined as any as (Record<string, any>), events: undefined as any as { [evt: string]: CustomEvent<any> }, slots: undefined as any as {}, bindings: undefined as any as string, exports: undefined as any as ({}) };
}
$$render_bf7123b7;
const __svn_component_default: import('svelte').Component<
    Awaited<ReturnType<typeof $$render_bf7123b7>>['props'],
    Awaited<ReturnType<typeof $$render_bf7123b7>>['exports'],
    ''
> = null as any;
type __svn_component_default = ReturnType<typeof __svn_component_default>;
export default __svn_component_default;

