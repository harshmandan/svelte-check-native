// === input.svelte ===
// generated by svelte-check-native; do not edit
declare const title: { [key: string]: any } & ((...args: any[]) => any);
    interface InnerProps {
        title: string;
    }
async function $$render_3a475a7e() {
    type $$ComponentProps = { props: InnerProps; title: string };

    // Threlte/theatre Declare.svelte pattern: the parent component
    // passes a prop literally named `props`, so `$props()` destructure
    // pulls a binding called `props`. Previously svn-lint fired a
    // false-positive `store_rune_conflict` here — "you're using `$props`
    // rune but there is a local binding called `props`" — even though
    // the user's `$props()` call is the rune itself, not a store sub of
    // a separate `props` variable. Verified against upstream
    // svelte/compiler 5.53.6 directly: this pattern produces ONLY a
    // `state_referenced_locally` warning, never `store_rune_conflict`.

                          
                      
     

    let { props, title }: /*Ωignore_startΩ*/$$ComponentProps/*Ωignore_endΩ*/ = $props();

    void props;
    void title;
    ;(async () => {
        // template type-check body (incremental)
        { svelteHTML.createElement("p", {});             (title);
        }
    });
    void props;
    void title;
    return { props: undefined as any as ($$ComponentProps), events: undefined as any as { [evt: string]: CustomEvent<any> }, slots: undefined as any as {}, bindings: undefined as any as string, exports: undefined as any as ({}) };
}
$$render_3a475a7e;
const __svn_component_default: import('svelte').Component<
    Awaited<ReturnType<typeof $$render_3a475a7e>>['props'],
    Awaited<ReturnType<typeof $$render_3a475a7e>>['exports'],
    ''
> = null as any;
type __svn_component_default = ReturnType<typeof __svn_component_default>;
export default __svn_component_default;

