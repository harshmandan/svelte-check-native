{
    "errors": [
        {
            "file": "input.svelte",
            "line": 2,
            "column": 8,
            "code": 6133
        },
        {
            "file": "input.svelte",
            "line": 3,
            "column": 10,
            "code": 2322
        }
    ],
    "_note": "Diagnostic positions must survive the post-body in-place rewrites. The script body's byte-precise token-map entry is pushed before the de-narrow trailer (` a = undefined as any;` after `let a: string | null = null;`) and the bind:this definite-assign `!` (`let el!: HTMLDivElement;`) are spliced into that span; the token map is re-anchored around each insertion, so the TS2322 on `const b: string = 1;` \u2014 which FOLLOWS both insertions \u2014 still maps to the declaration's exact source line/column (0-based 3:10, the `b` identifier). Pre-fix the 22-byte trailer shifted the reverse-mapped position onto the next line. Unused-variable reports on write-only bind targets are upstream's: every template read is copied into the render function, and no `void` list masks a binding the template only assigns."
}
