{
    "errors": [],
    "_note": "The file ends inside an opening tag, so the Svelte compiler's parse() throws. svelte-check --tsgo leaves a component whose parse fails out of the run entirely (svelte2tsx runs in strict mode), so nothing is reported for it."
}
