{
    "errors": [],
    "_note": "A stray `{/if}` makes the Svelte compiler's parse() throw. svelte-check --tsgo leaves a component whose parse fails out of the run entirely (svelte2tsx runs in strict mode), so nothing is reported for it."
}
