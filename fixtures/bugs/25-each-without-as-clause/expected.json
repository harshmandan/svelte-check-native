{
    "emit_not_contains": [
        "for (let  of",
        "for (let of "
    ],
    "emit_contains": [
        "for (let"
    ],
    "_note": "`{#each items}` (no `as` clause) generated `for (const  of ...)` \u2014 double space / missing binding \u2192 TS1005. Emit must use a placeholder binding. The loop variable is `let`, as upstream declares it."
}
