{
    "errors": [
        { "file": "input.svelte", "line": 2, "column": 10, "code": 6133 }
    ],
    "_note": "`{@debug a // note, y\\n}` names only `a`: the `, y` sits in a line comment. Splitting the body on commas used to emit `(a // note);` and `( y);`, a syntax error that hides every type error in the workspace and, with it, the unused-variable report on `y`."
}
