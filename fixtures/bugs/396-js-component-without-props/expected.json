{
    "errors": [
        {
            "file": "src/Parent.svelte",
            "line": 6,
            "column": 7,
            "code": 2322
        },
        {
            "file": "src/Parent.svelte",
            "line": 10,
            "column": 13,
            "code": 2322
        }
    ],
    "_note": "A JS component's props follow svelte2tsx's `createPropsStr`: with no exports (legacy) or no `$props()` type (runes) they are `Record<string, never>`, so a consumer's extra prop is rejected; a `@typedef` unrelated to the props never types them, and a runes component's leading `@type` comment does. Verified against svelte-check --tsgo."
}
