{
    "errors": [
        {
            "file": "ParentMismatch.svelte",
            "line": 12,
            "column": 19,
            "code": 2345
        }
    ],
    "_note": "v0.3 Item 5: Child uses `createEventDispatcher<DispatcherEvents>()` without an `interface $$Events`. Analyze's `find_dispatcher_event_type_source` extracts `DispatcherEvents` from the call's type arguments; emit synthesizes `type $$Events = DispatcherEvents;` at overlay module scope; Item 3's `& { readonly __svn_events: $$Events }` intersection fires; consumer's `__svn_ensure_component` typed overload narrows `$on` signatures; wrong-payload handler surfaces TS2345 at the `on:myevent={wrongHandler}` directive position (line 13, col 20 — position of `wrongHandler`)."
}
