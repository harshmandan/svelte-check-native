{
    "errors": [
        {
            "file": "input.svelte",
            "line": 5,
            "column": 14,
            "code": 2552
        }
    ],
    "_note": "Round 2 #3: {@debug a, b} now type-checks each comma-separated identifier. Pre-2026-04-26 the directive was a no-op in our emit, so typos like `undefinedTypo` silently passed. Code 2552 (not 2304) fires because tsgo suggests `undefined` as a similar name. Positions are LSP-style 0-based."
}
